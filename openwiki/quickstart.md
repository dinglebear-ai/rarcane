---
type: Playbook
title: OpenWiki Quickstart
description: Entry point for this repository's OpenWiki documentation. Explains where the autogenerated docs live, how updates are scheduled, and where to find operation details.
tags:
  - openwiki
  - documentation
  - arcane-rmcp
---

# OpenWiki Quickstart

This repository tracks source-of-truth documentation with OpenWiki. The canonical generated documentation set for this run lives under `openwiki/`, and updates are primarily driven by the [OpenWiki update workflow](./operations/openwiki-update-workflow.md).

Use this page as the start point whenever you need to understand:

- how OpenWiki runs automatically in CI, which is documented in [OpenWiki update workflow](./operations/openwiki-update-workflow.md),
- where to look after documentation-sensitive source changes.

## Core navigation

- [Operations: OpenWiki update workflow](./operations/openwiki-update-workflow.md)

## What changed recently

The current workflow runs on `ubuntu-latest`, executes `openwiki code --update --print` against OpenRouter (`OPENWIKI_PROVIDER=openrouter`, `OPENWIKI_MODEL_ID=z-ai/glm-5.2`), and posts generated documentation updates from the scoped pull-request paths `openwiki`, `AGENTS.md`, `CLAUDE.md`, and `.github/workflows/openwiki-update.yml`.

## Practical workflow for maintainers

When these source files change, keep edits scoped to the docs pages they affect: update the **OpenWiki operation playbook** rather than broad files.

For any future edit to `.github/workflows/openwiki-update.yml`, review [the related operation doc](./operations/openwiki-update-workflow.md) and refresh the directly affected sections.

If you only need to confirm OpenWiki settings for non-workflow edits, check the linked operation page before touching broader sections.

## Backlog

- `openwiki/operations` expansion for other documentation automation channels is not required for this release; no additional pending area is identified.
